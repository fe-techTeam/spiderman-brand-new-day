export const metadata = {
  title: "The Avatar Experience",
  description: "Answer a few questions and discover the Spider identity that has always been yours.",
};

export default function QuizLayout({ children }) {
  return children;
}
